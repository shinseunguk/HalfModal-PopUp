# Uncomment the next line to define a global platform for your project
# platform :ios, '9.0'

target 'TestProejct' do
  # Comment the next line if you don't want to use dynamic frameworks
  use_frameworks!

  # Pods for TestProejct
   pod 'SnapKit' # UI AutoLayout 라이브러리
   pod 'Then' # Then 라이브러리


  target 'TestProejctTests' do
    inherit! :search_paths
    # Pods for testing
  end

  target 'TestProejctUITests' do
    # Pods for testing
  end

end
